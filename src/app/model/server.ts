import { OSVersion } from './os-version';

export class Server {

    id: number;
    name: string;
    ip: string;
    osVersion: OSVersion;
    ram: number;
    cpu: number;

    search: string;

}
